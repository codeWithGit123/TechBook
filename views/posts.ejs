<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Posts</title>
  <link rel="shortcut icon" href="/images/favicon.jpg" type="image/x-icon">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

  <% if (r && r.length> 0) { %> <!-- Check if r is defined and has elements -->
    <% for (let i=0; i < r.length; i++) { %>
      <div class="card mb-3 my-4" style="width: 18rem;">
        <span class="badge text-bg-primary  my-2 mb-2">Posted At:
          <%= r[i].createdAt  %>
        </span>
        <span class="badge text-bg-primary  my-2 mb-2">Edited At:
          <%= r[i].updatedAt  %>
        </span>
        <img src="data:<%= r[i].contentType %>;base64,<%= r[i].img.toString(
        'base64' ) %>" alt="An Image" />
        <div class="card-body">
          <h5 class="card-title">
            <%= r[i].title %>
          </h5>
          <p class="card-text">
            <%= r[i].desc %>
          </p>
        </div>
        <div class="container-fluid">
          <div class="row">
            <div class="col">
              <a href="/edit/<%=r[i]._id%>"><button class="btn btn-primary">Edit</button></a>
            </div>
            <div class="col">
              <a href="/delete/<%=r[i]._id%>"><button class="btn btn-danger">Delte</button></a>
            </div>
          </div>
        </div>
      </div>

      <% } %>
        <% } else { %>
          <h3>No Posts</h3>
          <% } %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>