<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <link rel="shortcut icon" href="/images/favicon.jpg" type="image/x-icon">
        <title>Your Questions</title>
    </head>
    <body>
        <h2>Your Questions Will Be Displayed Here</h2>
        <% for( let i=0; i < q.length; i++ ) { %>
            <div class="card mb-3 my-4" style="width: 18rem;">
              <span class="badge text-bg-primary  my-2 mb-2">Posted At:
                <%= q[i].createdAt  %>
              </span>
              <span class="badge text-bg-primary  my-2 mb-2">Edited At:
                <%= q[i].updatedAt  %>
              </span>
              <h5 class="card-title">@<%= q[i].uname  %></h5>
              <div class="card-body">
                <h5 class="card-title">
                  <%= q[i].topic %>
                </h5>
                <p class="card-text">
                  <%= q[i].question %>
                </p>
              </div>
              <div class="container-fluid">
                <div class="row">
                  <div class="col">
                    <a href="/editq/<%=q[i]._id%>"><button class="btn btn-primary">Edit</button></a>
                  </div>
                  <div class="col">
                    <a href="/deleteq/<%=q[i]._id%>"><button class="btn btn-danger">Delte</button></a>
                  </div>
                </div>
              </div>
            </div>
            <% } %>
    </body>
</html>